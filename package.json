{
  "name": "pcs-collect-pmm-pgsql",
  "version": "2.0.0",
  "description": "Collect PMM/Grafana dashboard panels as screenshots using Playwright (no image-renderer plugin required)",
  "type": "module",
  "scripts": {
    "postinstall": "npx playwright install chromium"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "playwright": "^1.50.0",
    "tar": "^7.4.0"
  }
}
